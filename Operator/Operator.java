import java.util.*;

/**
 * Operator
 */
public class Operator {
    public static void main(String[] args) {
        // 按运算符从高到低
        // 从上到下进行

        /// 后缀运算符 () [] . 

        /// 一元运算符 ++ -- ！〜

        // 乘性  * /％

        // 加性 + -

        // 移位 >> >>>  << 
            // 注意 >>> 为无符号右移，全补为0
            // 而 >> 有符号右移，正数补为0，负数补为1
            // << 有符号左移，全补为0
        int yayaya = 1025;
        yayaya = yayaya << 1;
        System.out.print(yayaya);

        // 关系	> >= < <=
        
        // 相等 ==  !=

        // 按位与 &

        // 按位异或 ^

        // 按位或 |

        // 逻辑与 &&

        // 逻辑或 || 

        // 条件（三元运算符） ? :

        // 赋值 = += -= *= /= ％= >>= < = ＆= ^= |=

        // 逗号 ,
    }
}